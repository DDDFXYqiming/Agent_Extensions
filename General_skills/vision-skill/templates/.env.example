# 视觉模型配置：复制本文件为技能目录下的 .env，脚本启动时自动加载
# 也可直接导出为环境变量；环境变量优先级高于 .env 文件
# 所有具体参数留空，按你的环境填写；不要提交真实 Key 到仓库
VISION_API_URL=  # 完整接口地址（含路径），如 https://api.example.com/v1/chat/completions
VISION_MODEL=
VISION_API_KEY=
