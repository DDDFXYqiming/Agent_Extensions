# Bulk visual enrichment

Use SKILL.md's complete-transcript and adaptive visual workflow for each verified video in a separate directory. Three frames at 20/50/80 percent are a legacy preview, not sufficient evidence by default.

Reuse media and manifests, review overview sheets and important originals, supplement uncertain intervals, and persist each video's evidence and final notes. Resume only missing/failed work. Old chunk_summaries may be truncated prefixes: recover full segments before summarizing.

Rebuild conclusions from text and images; do not only append captions to old transcript-only notes. Regenerate the uploader index from verified final notes; the index need not repeat every analyzed screenshot. Check identity, links, coverage, unresolved questions and artifact counts. Counts detect missing files, not semantic accuracy.
